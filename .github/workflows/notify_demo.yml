name: Notify Demo
run-name: ${{ github.actor }} is Pushed

on: [workflow_dispatch]

jobs:
  run:
    runs-on: ubuntu-latest
    env:
      BRANCH: main
    steps:
      - name: echo "${{ env.BRANCH }}"
        uses: actions/checkout@v4
        with:
          ref: ${{ env.BRANCH }}
